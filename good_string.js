// a good string is a string that starts with the letter 'a' & and has length >= 5

let word  = "apple" ;

if ((word[0] == 'a' ) && (word.length >=5) ){
    console.log(`Good String`);
}
else {
    console.log(`Not a Good String`) ;
}
